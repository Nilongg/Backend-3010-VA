-- Retrieve names and lengths of all tracks. Sort the result rows in
-- ascending order by the length column.

SELECT name, length FROM track ORDER BY length ASC;
--Tehnyt ryhmässä (Niilo V, Mikael N, Aleksi O)